@model EczaneOtomasyonu.Models.Ilaclar

@{
    ViewData["Title"] = "Yeni İlaç Ekle";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg border-0 rounded-4">
                <div class="card-header bg-primary text-white text-center py-3 rounded-top-4">
                    <h4 class="mb-0 fw-bold">💊 Yeni İlaç Sistemi Girişi</h4>
                </div>
                <div class="card-body p-5 bg-light rounded-bottom-4">

                    <form asp-action="Create">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="row">
                            <div class="col-md-6 mb-4">
                                <label asp-for="BarkodNo" class="form-label fw-bold text-secondary">Barkod No</label>
                                <input asp-for="BarkodNo" class="form-control form-control-lg shadow-sm border-0" placeholder="Örn: 869001" autocomplete="off" />
                                <span asp-validation-for="BarkodNo" class="text-danger small"></span>
                            </div>

                            <div class="col-md-6 mb-4">
                                <label asp-for="IlacAdi" class="form-label fw-bold text-secondary">İlaç Adı</label>
                                <input asp-for="IlacAdi" class="form-control form-control-lg shadow-sm border-0" placeholder="Örn: Parol 500mg" autocomplete="off" />
                                <span asp-validation-for="IlacAdi" class="text-danger small"></span>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-4">
                                <label asp-for="BirimFiyat" class="form-label fw-bold text-secondary">Birim Fiyatı (₺)</label>
                                <input asp-for="BirimFiyat" type="text" class="form-control form-control-lg shadow-sm border-0" placeholder="Örn: 45.50" />
                                <span asp-validation-for="BirimFiyat" class="text-danger small"></span>
                            </div>

                            <div class="col-md-6 mb-5">
                                <label asp-for="KategoriId" class="form-label fw-bold text-secondary">İlaç Kategorisi</label>
                                <select asp-for="KategoriId" class="form-select form-select-lg shadow-sm border-0" asp-items="ViewBag.KategoriId">
                                    <option value="">-- Kategori Seçiniz --</option>
                                </select>
                                <span asp-validation-for="KategoriId" class="text-danger small"></span>
                            </div>
                        </div>

                        <div class="d-flex justify-content-between align-items-center mt-2 border-top pt-4">
                            <a asp-action="Index" class="btn btn-outline-secondary btn-lg px-4 fw-bold shadow-sm">🔙 Listeye Dön</a>
                            <button type="submit" class="btn btn-success btn-lg px-5 fw-bold shadow-sm">💾 İlacı Kaydet</button>
                        </div>
                    </form>

                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}